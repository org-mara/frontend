'use strict';

/**
 * @ngdoc function
 * @name frontendApp.controller:ComuCtrl
 * @description
 * # ComuCtrl
 * Controller of the frontendApp
 */
angular.module('frontendApp')
  .controller('ComuCtrl', function ($scope) {
  
  });
