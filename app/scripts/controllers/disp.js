'use strict';

/**
 * @ngdoc function
 * @name frontendApp.controller:DispCtrl
 * @description
 * # DispCtrl
 * Controller of the frontendApp
 */
angular.module('frontendApp')
  .controller('DispCtrl', function ($scope) {
  
  });
